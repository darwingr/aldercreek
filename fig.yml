web:
  build: .
  ports:
    - "80:80"
  links:
    - db:mysql
  volumes:
    - /Users/darwingroskleg/Developer/aldercreek/wp-data:/var/www/html
db:
  image: mysql
  environment:
    - MYSQL_DATABASE=wordpress
    - MYSQL_ROOT_PASSWORD=darweb