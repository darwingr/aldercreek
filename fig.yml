# fig version 1.0.1 (docker-compose)
# docker version 1.4.1
web:
  build: .
  ports:
    - "80:80"
  links:
    - db:mysql
  volumes:
    - /aldercreek/aldercreek-wp:/var/www/html
db:
  image: mysql:latest
  environment:
    - MYSQL_DATABASE=wordpress
    - MYSQL_ROOT_PASSWORD=***REMOVED***
